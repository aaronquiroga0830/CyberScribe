# Core
langchain>=0.3.0
langchain-community>=0.3.0

# Local LLM (Ollama)
# Ollama runs separately; no extra package required for HTTP calls

# Vector store: FAISS (MVP). Swap to Chroma later by installing chromadb.
faiss-cpu>=1.7.4

# Embeddings (local, same machine as Ollama)
sentence-transformers>=2.2.0

# Document loaders (PDF, optional .docx)
pypdf>=3.0.0

# Rich report output (.docx)
python-docx>=1.0.0

# Scheduler (daily pipeline over months)
apscheduler>=3.10.0

# Web server (API + Liquid Glass UI, live SSE streaming)
fastapi>=0.104.0
uvicorn>=0.24.0
bcrypt>=4.0.0
xhtml2pdf>=0.2.11

# Utilities
python-dotenv>=1.0.0
requests>=2.28.0

# Optional: uncomment when moving to Chroma for daily updates
# chromadb>=0.4.0

# Optional: for .docx support in loaders, install: pip install unstructured
