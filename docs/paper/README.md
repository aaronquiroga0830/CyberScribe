# Paper text for benchmark results

## `benchmark_results_section.txt`

Plain-text section to copy into your LaTeX paper (headings, table, figure caption, limitations).

Figure: `output/benchmark/experiment2/fig_paper_pipeline_phases.pdf`

After the 10-trial run completes, fill in `[pending]` values from:

- `output/benchmark/experiment2/summary_table.csv`
- `output/benchmark/experiment2/accuracy_summary_table.csv`

Legacy LaTeX version (optional): `benchmark_results_section.tex`
